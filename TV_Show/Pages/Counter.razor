@page "/counter"

<h1>Counter</h1>

<p>Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

<br />

@*<button class="btn btn-primary" @onclick="FromBlazorToDB1">Add Series</button>*@


@code {
    private int currentCount = 0;

    [Parameter] public Serials Serials { get; set; }

    private void IncrementCount()
    {
        currentCount++;
    }
    //private void FromBlazorToDB1()
    //{
    //    Serials.AddSeriesToDb(new Serials(Serials.SerialName, Serials.SerialSeason, Serials.SerieslNumber, Serials.SerieslName));
    //}
}
